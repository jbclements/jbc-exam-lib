#lang setup/infotab

(define name "jbc-exam-lib")

(define collection "jbc-exam-lib")

(define deps '("slatex"
               "sxml"
               "typed-racket-lib"
               "base"
               "rackunit-lib"
               "rackunit-typed"))

(define build-deps '("at-exp-lib"))
